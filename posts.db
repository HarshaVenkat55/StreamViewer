CREATE DATABASE streamviewerdb;

CREATE TABLE posts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(200),
    msg VARCHAR(200),
    videoId VARCHAR(200),
    date TIMESTAMP NOT NULL DEFAULT CURRENT_DATE());
